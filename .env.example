# GitHub API Configuration
BASE_URL=https://api.github.com

# Your GitHub username
USERNAME=tu_usuario_github

# Repository name (must exist if you use fixed PR tests, or can be created dynamically)
REPO_NAME=tu_repositorio_de_pruebas

# Personal Access Token (classic) with scopes: repo, user, delete_repo
ACCESS_TOKEN=ghp_tu_token_aqui