name: CI/CT Pipeline
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  lint:
    name: Linter (flake8)
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pep8-naming
    - name: Run Flake8
      run: flake8 . --count --statistics --max-line-length=88

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    needs: lint
    env:
      BASE_URL: ${{ secrets.BASE_URL }}
      USERNAME: ${{ secrets.USERNAME }}
      REPO_NAME: ${{ secrets.REPO_NAME }}
      ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt pytest-xdist
    - name: Run Unit Tests
      run: |
        python -m pytest tests/unit/ -v

  functional-tests:
    name: Tests
    runs-on: ubuntu-latest
    needs: unit-tests
    env:
      BASE_URL: ${{ secrets.BASE_URL }}
      USERNAME: ${{ secrets.USERNAME }}
      REPO_NAME: ${{ secrets.REPO_NAME }}
      ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt pytest-cov pytest-html pytest-xdist
- name: Run Tests
      run: |
        mkdir -p reports

        python -m pytest tests/unit/ -v \
          --junitxml=reports/junit_unit.xml \
          --cov=services/ \
          --cov=utils/ \
          --cov-report=xml:reports/coverage.xml \
          --cov-report=html:reports/coverage

        python -m pytest tests/ --ignore=tests/unit/ -v \
          --html=reports/report.html \
          --self-contained-html \
          --junitxml=reports/junit_ct.xml \
          --cov=services/ \
          --cov=utils/ \
          --cov-append \
          --cov-report=xml:reports/coverage.xml \
          --cov-report=html:reports/coverage \
          --cov-fail-under=80
    - name: Upload Reports
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: pytest-ct-reports-run-${{ github.run_number }}
        path: reports/
        retention-days: 30